import React from "react";
import ReactDOM from "react-dom/client";
import Facebird from "./Facebird";
import "./style.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Facebird />
  </React.StrictMode>
);
